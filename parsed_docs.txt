Multi-Agent Equity Research SystemAutomated Indian Stock Research System — System Overview & Build PlanMulti-Agent Equity Research SystemAutomated Indian Stock Research System — System Overview & Build PlanWhat We Are BuildingAn end-to-end automated investment research platform for the Indian stock market. Every week, the system analyses 800 NSE/BSE stocks using a combination of deterministic financial filters and AI agents, producing scored investment theses for each stock — exactly the kind of work a team of equity analysts would do manually, done fully automatically.The output is a ranked, scored universe of Indian stocks — each with a composite score out of 100, a bull/base/bear target price, and a 150-word investment thesis — accessible via API, dashboard, and Telegram bot.How It Works762000255831Step 1 — Data Ingestion (automatic, daily/weekly)Step 1 — Data Ingestion (automatic, daily/weekly)Financial data (P&L, Balance Sheet, Cash Flow, Ratios) pulled from yfinance and stored as structured JSONB in SupabaseConcall transcripts and investor PPTs scraped from Screener.in, chunked into ~500-token segments, embedded using Voyage-3, and stored in Supabase with pgvector762000224628Step 2 — Tier 1 Prefilter (Python only, zero AI cost)Step 2 — Tier 1 Prefilter (Python only, zero AI cost)All 800 stocks run through hard rules: revenue growth ≥ 12%, no auditor resignation, no promoter pledge increase, liquidity checksTechnical sub-scores computed deterministically using TA-Lib (RSI, MACD, 200-DMA)~200 stocks pass through to AI analysis. ~600 get a rank-only update. Cost: ₹0762000224628Step 3 — AI Agent Analysis (LangGraph, 4 parallel agents per stock)Step 3 — AI Agent Analysis (LangGraph, 4 parallel agents per stock)Agent A — Fundamentals: scores Growth (30%), Durability (20%), Management Quality (20%) from financial modelsAgent B — Sector Specialist: one of 10 sector-specific instances (BFSI, IT, Pharma, FMCG, etc.) assessing sector-specific KPIsAgent C — Management Sentiment: RAG over concall transcripts, detects tone shifts, guidance changes, red flagsAgent D — Valuation & Risk: bull/base/bear target prices, risk flags, liquidity and accounting checks762000224197Step 4 — Synthesis & Output (Claude Opus 4.7)Step 4 — Synthesis & Output (Claude Opus 4.7)Synthesis Agent combines all 4 outputs using locked weights: Growth 30% + Durability 20% + Mgmt Quality 20% + Sentiment 10% + Valuation 10% + Technical 10%Override rules applied (e.g. any sub-score < 35 caps at HOLD)Generates 150-word thesis, selects key risks and catalysts, finalises target pricesResults stored in Supabase, accessible via FastAPI, dashboard, and Telegram botTechnology StackDatabaseSupabase (PostgreSQL + pgvector)AI AgentsLangGraph + Claude Sonnet 4.6SynthesisClaude Opus 4.7EmbeddingsVoyage-3Task QueueCelery + RedisSchedulerCelery Beat (weekly cron)API LayerFastAPI + UvicornFinance Datayfinance + TA-LibNLP / ScrapingPyMuPDF + BeautifulSoupDeploymentRailway / RenderAI CodingClaude Code (primary tool)NotificationsTelegram BotBuild PlanTwo-person parallel execution — complete system in 7 daysBuild PlanTwo-person parallel execution — complete system in 7 daysBoth developers work in parallel from Day 1. Use Claude Code for all boilerplate, SQLAlchemy models, FastAPI routes, and agent scaffolding. Test with 10 stocks before scaling. Never write a function from scratch that Claude Code can generate in 30 seconds.762000268773Pre-work — Before Day 1 (1–2 hours, both together)Pre-work — Before Day 1 (1–2 hours, both together)Do this together before splitting work:Create Supabase project. Run CREATE EXTENSION IF NOT EXISTS vector; in SQL editor.Set Anthropic API spend cap in console (set low — ₹3,000 — raise it when agents are stable).Both pull a stock from yfinance in a notebook and make one raw Claude API call. Understand the input/output before writing a single pipeline function.Create shared Git repo. Agree on branch naming: feature/db-setup, feature/prefilter, etc.Days 1–2 — FoundationPerson A — Database + Data IngestionDay 1: Write all SQLAlchemy table models using Claude Code (stocks, financial_models, documents, document_chunks, analysis_runs, stock_analysis). Run Alembic migrationsagainst Supabase pooler (port 6543).Day 1: yfinance collector — ingest 50 Nifty 500 stocks. Verify rows appear in Supabase Table Editor.Day 2: Screener.in scraper — download concall PDFs + investor PPTs. Extract text with PyMuPDF. Store raw docs in documents table.Day 2: Test full ingestion end-to-end:stock data + document metadata stored, readable.Person B — AI Pipeline SetupDay 1: Set up FastAPI skeleton with/health endpoint. Confirm it runs. Wire Supabase connection using same pooler URL.Day 1: PyMuPDF text cleaner pipeline — extract, clean, chunk concall text into~500-token segments. Test on 3 real PDFs.Day 2: Voyage-3 embedding pipeline — embed chunks, upsert into document_chunks table with pgvector.Run one similarity search query to confirm it works.Day 2: Set up Redis locally. ConfirmCelery worker connects and processes a test task.Days 3–4 — Tier 1 Prefilter + First AgentPerson A — Tier 1 PrefilterDay 3: Implement all hard filter rules in pure Python/Pandas — revenue growth, auditor flag, pledge increase, liquidity,latest financials within 90 days. One function per rule.Person B — Fundamentals Agent (Agent A)Day 3: Define Pydantic output schema for Agent A — growth_score,durability_score, mgmt_quality_score, paragraphs, internal_tensions[].Day 3: Add TA-Lib technical sub-scores — RSI, MACD, 200-DMA position, RS rank vs Nifty 500.Day 4: Run prefilter on all 50 ingested stocks. Read every pass/fail result — verify they make logical sense.Day 4: Write tier_reached scores back to Supabase stock_analysis table. Confirm in dashboard.Day 3: Write Agent A prompt usingClaude Code. Test on one real stock's financial data. Verify structured output matches schema exactly.Day 4: Add prompt caching (cache_control blocks on system prompt and financial context) — critical for cost control at scale.Day 4: Write agent output to analysis_runs and stock_analysis tables. Confirm end-to-end for 3 stocks.Days 5–6 — All 4 Agents + LangGraphPerson A — Sector Specialist + ScoringDay 5: Build Agent B (Sector Specialist)— start with 3 sector instances (BFSI, IT, Consumer). Use Claude Code to generate the remaining 7 sector system prompts from the spec.Day 5: Build Agent D (Valuation & Risk) — target prices, risk flags, severity ranking. Pydantic output schema first.Day 6: Wire all 4 agents into LangGraph with parallel Send() execution. Orchestrator routes by sector.Day 6: Add Synthesis Agent (Opus 4.7). Implement composite score formula and all override rules as Python if/else blocks.Person B — Management Sentiment + DB WriterDay 5: Build Agent C (ManagementSentiment RAG) — pull latest 2–4 concall chunks per stock via pgvector similarity search, detect tone shifts and guidance changes.Day 5: Add QoQ comparison logic —compare current sentiment output against prior quarter stored in stock_analysis.Day 6: Build DB Writer node — upsert ViewObject to stock_analysis, refresh latest_scores_view materialized view, write audit trail.Day 6: Run full pipeline (prefilter → all 4 agents → synthesis → DB write) on 10 real stocks. Fix every failure.Day 7 — Integration, API & DeploymentPerson A — Scale + SchedulerScale pipeline from 10 → 50 → 100 stocks. Each step reveals different bugs (rate limits, DB connections, timeouts). Fix before moving up.Set up Celery Beat cron: Sunday 22:00 IST full-universe run. Test at 5-minute interval first.Add rate limit state tracking via Redis(prevent Anthropic API burst failures on large runs).Set up Supabase keep-alive GitHub Action— pings DB every 3 days to prevent free-tier auto-pause.Person B — API + DemoBuild FastAPI endpoints: /view/{ticker},/top, /refresh/{ticker}, /views/top. Claude Code generates these from SQLAlchemy models.Deploy app to Railway or Render. Point environment variables at Supabase pooler URL and Redis.Optional: Telegram bot — /top command returns top 5 picks. High impact forstakeholder demo.End-of-day demo run: trigger full pipeline on 50 stocks, read results via /top endpoint. That is your MVP.762000236897Speed rules — how to actually finish in 7 daysSpeed rules — how to actually finish in 7 daysUse Claude Code for every function — never write boilerplate from scratch. Schema, model, route, prompt — all generated.Test fixtures first — save 3 real stock JSON responses and 2 concall chunks as local files. Run all agent tests against these, not live APIs.One feature per branch — merge daily. Never let work accumulate across 2+ days.Time-box every task to half a day. Stuck after 4 hours? Skip it, flag it, move on.Sync twice daily — 5-minute check-in morning and evening. Three lines: what I did, what I'm doing, what is blocked.Scale last — build and test with 10 stocks. Only touch 800 in the final day.Pydantic first — define output schema before writing any agent prompt. Schema drives the prompt, not the other way around.End-of-Week Deliverable TargetsDay 2Supabase populated with 50 stocks. Documents ingested. Embeddings stored.Day 4Prefilter running on real data. Agent A producing structured scores.Day 6All 4 agents + synthesis working on 10 stocks end-to-end.Day 7Full pipeline deployed. /top API returning real picks. MVP complete.